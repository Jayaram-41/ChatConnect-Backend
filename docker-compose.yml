services:
  backend:
    build: .
    container_name: chatconnect-backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=${DATABASE_URL:-postgresql://postgres:[YOUR-PASSWORD]@db.ahchzeygwalxoularnyn.supabase.co:5432/postgres}
    env_file:
      - .env
